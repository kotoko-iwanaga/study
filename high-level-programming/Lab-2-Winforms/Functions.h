#pragma once
#include <vector>

bool rowByDecrease(std::vector< std::vector<int> >&, int);
int countRows(std::vector< std::vector<int> >&);
void resizeMatrix(std::vector<std::vector<int>>&, int, int);